<!DOCTYPE html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Kelola Produk</title>@vite(['resources/css/app.css'])</head><body class="bg-slate-100 text-slate-900"><header class="border-b bg-white"><div class="mx-auto flex h-16 max-w-7xl items-center justify-between px-5"><div><b>secondbyme.</b><span class="ml-3 text-xs text-slate-400">Admin Panel</span></div><div class="flex items-center gap-4"><a href="{{ url('/') }}" target="_blank" class="text-sm font-bold text-slate-500">Lihat Website</a><form method="POST" action="{{ route('admin.logout') }}">@csrf<button class="text-sm font-bold text-red-600">Keluar</button></form></div></div></header><main class="mx-auto max-w-7xl px-5 py-10"><div class="flex items-end justify-between"><div><p class="text-sm font-bold text-lime-700">Katalog</p><h1 class="mt-1 text-3xl font-black">Kelola Produk</h1></div><a href="{{ route('admin.products.create') }}" class="rounded-xl bg-[#171816] px-5 py-3 text-sm font-black text-white">+ Tambah Produk</a></div>@if(session('success'))<p class="mt-6 rounded-xl bg-emerald-100 p-4 text-sm font-bold text-emerald-700">{{ session('success') }}</p>@endif<div class="mt-7 overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="w-full min-w-[850px] text-left"><thead class="border-b bg-slate-50 text-xs uppercase text-slate-400"><tr><th class="p-4">Produk</th><th>Harga</th><th>Stok</th><th>Status</th><th class="pr-4 text-right">Aksi</th></tr></thead><tbody>@forelse($products as $product)<tr class="border-b last:border-0"><td class="p-4"><div class="flex items-center gap-3"><img src="{{ $product->image_url }}" class="h-14 w-14 rounded-xl object-cover"><div><b>{{ $product->name }}</b><p class="text-xs text-slate-400">{{ $product->storage }} · {{ $product->color }}</p></div></div></td><td class="font-bold">Rp {{ number_format($product->price,0,',','.') }}</td><td><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $product->stock ? 'bg-emerald-100 text-emerald-700' : 'bg-red-100 text-red-600' }}">{{ $product->stock }} unit</span></td><td>{{ $product->is_active ? 'Tampil' : 'Disembunyikan' }}</td><td class="pr-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.products.edit',$product) }}" class="rounded-lg bg-slate-100 px-3 py-2 text-xs font-bold">Edit</a><form method="POST" action="{{ route('admin.products.destroy',$product) }}" onsubmit="return confirm('Hapus produk ini?')">@csrf @method('DELETE')<button class="rounded-lg bg-red-50 px-3 py-2 text-xs font-bold text-red-600">Hapus</button></form></div></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-400">Belum ada produk.</td></tr>@endforelse</tbody></table></div></main></body></html>
